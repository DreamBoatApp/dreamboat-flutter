rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Authenticated users can read (view dream images)
      allow read: if request.auth != null;
      
      // Client never writes — all uploads via Admin SDK (Cloud Functions)
      allow write: if false;
    }
  }
}
